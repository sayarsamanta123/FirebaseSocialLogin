<resources>

    <!-- Base application theme. -->
    <style name="MyMaterialTheme" parent="MyMaterialTheme.Base">

    </style>

    <style name="MyMaterialTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/faveo_blue</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="actionBarTheme">@style/AppThemeOverlay.ActionBar</item>


    </style>

    <style name="AppThemeOverlay.ActionBarMainActivity" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="colorControlNormal">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/faveo</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="actionModeCloseDrawable">@color/white</item>
        <item name="actionModeCloseButtonStyle">@style/Widget.AppCompat.ActionMode</item>
    </style>
    <style name="AppThemeOverlay.ActionBar" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="colorControlNormal">@color/faveo</item>
        <item name="android:textColorPrimary">@color/faveo</item>
        <item name="android:textColorSecondary">@color/faveo</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="actionModeCloseDrawable">@color/white</item>
        <item name="actionModeCloseButtonStyle">@style/Widget.AppCompat.ActionMode</item>
    </style>

</resources>
